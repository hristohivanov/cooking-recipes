{# templates/home.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <div {{ react_component('Home', { 'loggedUser': loggedUser ? {
        'id': loggedUser.id,
        'username': loggedUser.username,
        'email': loggedUser.email
    }: null }) }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>

{% endblock %}